---
"@datocms/cma-schema-types-generator": minor
---

Fetch only the schema attributes that the generator uses. On projects with many models and fields, generating the types is now much faster and uses less memory.
